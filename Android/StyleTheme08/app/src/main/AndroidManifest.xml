<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kosmo.styletheme08">
    <!--원래 테마:android:theme="@style/Theme.StyleTheme08"-->
    <!--1. 기존 테마로 타이틀바 없애기:@style/Theme.MaterialComponents.NoActionBar-->
    <!--2. 내가만든 테마 적용  android:theme="@style/ThemeMadeByMe"
        앱의 배경색  변경시 activity_main.xml에서 가장 바깥의 레이아웃에
        아래 속성 추가
        예:android:background="#000000"
    -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StyleTheme08"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>